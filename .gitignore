# --- Instructor-only materials: NOT distributed to students ---
# Slides and demo notebooks stay local; only the lab itself is shared.
*.pptx
*.ppt
*.ipynb
.ipynb_checkpoints/
*.SAMPLE.docx

# --- Python noise ---
__pycache__/
*.py[cod]
.venv/
venv/

# --- Editor / OS noise ---
.vscode/
.idea/
.DS_Store
Thumbs.db

# NOTE: charts/ and its README.txt ARE tracked, but the folder ships almost
# empty on purpose -- the PNGs in it should be the student's own, made on their
# own machine. Their charts stay tracked so they can commit them as evidence.
